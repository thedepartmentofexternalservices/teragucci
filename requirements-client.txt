# Teraguchi Client Requirements (Mac / Windows / Linux)
# Install: pip install -r requirements-client.txt

websockets>=12.0
PySide6>=6.6

# Video decode (H.264/H.265/AV1 with hardware acceleration)
av>=12.0
numpy>=1.24  # Required by PyAV frame.to_ndarray() — client is black without it

# QUIC transport (optional, recommended)
aioquic>=1.0

# Phase 1 runtime additions (STAB-06 FSM library, OBS-01 structured logging)
python-statemachine>=2.6,<3
structlog>=25.1,<26
