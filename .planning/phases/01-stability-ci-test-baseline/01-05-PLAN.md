---
phase: 01-stability-ci-test-baseline
plan: 05
type: execute
wave: 2
depends_on: [03]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/build-artifacts.yml
  - packaging/teraguchi-server.spec
autonomous: true
requirements: [STAB-02, STAB-03]
threats_addressed: []
must_haves:
  truths:
    - "Every PR to dev or main triggers CI on macos-14 runner AND rockylinux:9 container"
    - "CI runs: pytest (all tests with --timeout=60) + ruff check + ruff format --check + mypy on common/ + client/protocol.py"
    - "Build artifacts gate runs PyInstaller .app build on macos-14 and rpmbuild --nodeps --buildonly on rockylinux:9"
    - "Rocky 9 RPM spec (packaging/teraguchi-server.spec) builds cleanly via dry-run (no real signing; DIST-02 handles Phase 6 signing)"
    - "All FOUR D-08 hard-gate job names (pytest, lint-type, build-artifacts, latency-bench) are present in ci.yml from first commit so D-07 branch protection can reference them immediately; Plan 16 replaces the latency-bench placeholder body with the real benchmark"
    - "The latency-bench placeholder installed here is a synthetic-harness-math stub (per D-09), not real-hardware timing — real-hardware latency is measured manually on DXS workstations pre-release"
    - "Any red CI job blocks merge (enforced by branch protection — set up by user via GitHub UI after this plan lands)"
    - "All GHA actions pinned to major versions (actions/checkout@v4, actions/setup-python@v5, actions/cache@v4, actions/upload-artifact@v4)"
    - "Rocky 9 container uses EPEL to install python3.12 (not 3.9 default)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "pytest + ruff + mypy + latency-bench placeholder on every PR; four jobs (test-macos, test-linux, lint-typecheck, latency-bench)"
      contains: "runs-on: macos-14"
    - path: ".github/workflows/build-artifacts.yml"
      provides: "PyInstaller .app dry-run on macos-14; rpmbuild --buildonly on rockylinux:9"
      contains: "pyinstaller"
    - path: "packaging/teraguchi-server.spec"
      provides: "Minimal RPM spec for Rocky 9 server — dry-run buildable; no signing"
      contains: "Name:           teraguchi-server"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml [tool.pytest.ini_options] + [tool.ruff] + [tool.mypy]"
      via: "pip install -r requirements-dev.txt + python3 -m pytest / ruff / mypy — tools read their config from pyproject.toml"
      pattern: "python3\\.12 -m pytest"
    - from: ".github/workflows/build-artifacts.yml"
      to: "packaging/teraguchi-server.spec"
      via: "rpmbuild -bb --nodeps --buildonly packaging/teraguchi-server.spec"
      pattern: "rpmbuild"
    - from: ".github/workflows/ci.yml::latency-bench (placeholder)"
      to: ".github/workflows/ci.yml::latency-bench (real benchmark, Plan 16)"
      via: "Plan 16 replaces the stub body with the synthetic-stage-time accumulator test; the job NAME and file stay constant so D-07 branch protection never needs reconfiguration"
      pattern: "latency-bench:"
---

<objective>
STAB-02 + STAB-03: Install GitHub Actions CI on the two runners locked by D-05 (macos-14 + rockylinux:9 Docker on ubuntu-latest). Four hard gates per D-08 — pytest, ruff+mypy, build artifacts, latency benchmark. This plan installs ALL FOUR gate job names so D-07 branch protection can reference the complete set immediately; the latency-bench job body is a placeholder stub (exit 0 with a "PENDING — Plan 16 replaces this" message), and Plan 16 replaces the stub body with the real synthetic-stage-time accumulator.

**Why the placeholder matters (D-07 + D-08):** If branch protection is enabled before the latency-bench job exists, the protection rule would need to be reconfigured once Plan 16 lands — an operational hazard. By installing the job NAME here from the first ci.yml commit, D-07 can select all four required jobs immediately and Plan 16 is a zero-config swap of the job body.

**Why it's synthetic (D-09):** The latency-bench placeholder (and its Plan 16 real version) is a synthetic harness-math gate. Real-hardware latency is measured manually on DXS workstations pre-release. The YAML comment in the job block documents this explicitly.

Planner-open item RESOLVED here: Phase 1 ships a minimal `packaging/teraguchi-server.spec` that `rpmbuild --nodeps --buildonly` produces without errors (RESEARCH Open Q #2 recommendation). Full signing + GPG keys live in Phase 6 DIST-02.

Two tasks because CI workflow authoring and RPM spec authoring have different research surfaces — splitting keeps each task under ~20% context.

Purpose: Close the "zero CI today" blocker (STAB-02/03). Every subsequent plan's merge depends on green CI. D-07 branch protection must include all 4 gate job names (pytest, lint-type, build-artifacts, latency-bench) from the first ci.yml commit.
Output: Two GHA workflow files + minimal RPM spec; first green CI run on the branch after this plan lands, including the placeholder latency-bench job exiting 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stability-ci-test-baseline/01-CONTEXT.md
@.planning/phases/01-stability-ci-test-baseline/01-RESEARCH.md
@.planning/phases/01-stability-ci-test-baseline/01-PATTERNS.md
@pyproject.toml
@requirements-dev.txt
@install-server.sh
@build_client.py

<interfaces>
<!-- Relevant anchors from RESEARCH (lines 733-836) and PATTERNS (§"No Analog Found"). -->

From RESEARCH §"GitHub Actions ci.yml skeleton" lines 734-836 — pastable skeleton for the executor to adapt.

From PATTERNS §"Shell installer idioms" lines 1131-1139 — shell conventions to reuse for any pre/post scripts in the RPM spec (#!/usr/bin/env bash + set -euo pipefail).

From install-server.sh (top of file, read at task time) — the deps list that the RPM spec Requires: block mirrors.

Current state: no .github/ directory exists; no packaging/ directory exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <id>1-05-01</id>
  <name>Task 1: Create .github/workflows/ci.yml — pytest + ruff + mypy + latency-bench placeholder on macos-14 + rockylinux:9</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - /Users/randymcentee/workspace/GitHub/teraguchi/.planning/phases/01-stability-ci-test-baseline/01-RESEARCH.md §"GitHub Actions ci.yml skeleton" lines 734-836 — paste and adapt
    - /Users/randymcentee/workspace/GitHub/teraguchi/.planning/phases/01-stability-ci-test-baseline/01-RESEARCH.md §"Pitfall 5" lines 536-550 — Rocky 9 EPEL pitfall
    - /Users/randymcentee/workspace/GitHub/teraguchi/.planning/phases/01-stability-ci-test-baseline/01-RESEARCH.md §"Dependencies & Versions" Action table lines 1094-1103 — pinned action versions
    - /Users/randymcentee/workspace/GitHub/teraguchi/pyproject.toml (the tool config this CI invokes)
    - /Users/randymcentee/workspace/GitHub/teraguchi/requirements-dev.txt (the install target)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml`. Paste the RESEARCH skeleton and adapt to ship exactly FOUR jobs: `test-macos`, `test-linux`, `lint-typecheck`, and `latency-bench` (placeholder stub — Plan 16 replaces its body with the real synthetic-stage-time benchmark).

    Full contents:
    ```yaml
    name: CI
    on:
      pull_request:
        branches: [dev, main]
      push:
        branches: [dev, main]

    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    jobs:
      lint-typecheck:
        name: Lint + typecheck (ruff + mypy)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-python@v5
            with:
              python-version: "3.12"
          - uses: actions/cache@v4
            with:
              path: ~/.cache/pip
              key: pip-lint-${{ hashFiles('requirements-*.txt') }}
          - name: Install deps
            run: |
              python3 -m pip install --upgrade pip
              python3 -m pip install -r requirements-dev.txt
          - name: ruff check
            run: python3 -m ruff check .
          - name: ruff format --check
            run: python3 -m ruff format --check .
          - name: mypy
            run: python3 -m mypy

      test-linux:
        name: Tests (Rocky 9 + Python 3.12)
        runs-on: ubuntu-latest
        container:
          image: rockylinux:9
        steps:
          - uses: actions/checkout@v4
          - name: Install OS deps
            # RESEARCH Pitfall 5: Rocky 9's default python3 is 3.9. EPEL is required
            # for python3.12. ffmpeg-free covers the D-02 mocked-at-subprocess boundary
            # (we never actually encode on the runner).
            run: |
              dnf -y install epel-release
              dnf -y install python3.12 python3.12-devel python3.12-pip gcc git \
                             ffmpeg-free xclip pulseaudio-utils
          - name: Install Python deps
            run: |
              python3.12 -m pip install --upgrade pip
              python3.12 -m pip install -r requirements-dev.txt
          - name: Pytest
            # --timeout=60 overrides pyproject.toml's 30s default for the linux suite
            # which includes integration tests.
            run: python3.12 -m pytest -v --timeout=60

      test-macos:
        name: Tests (macOS 14 / Apple Silicon)
        runs-on: macos-14
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-python@v5
            with:
              python-version: "3.12"
          - uses: actions/cache@v4
            with:
              path: ~/.cache/pip
              key: pip-macos-${{ hashFiles('requirements-*.txt') }}
          - name: Install ffmpeg (for decoder tests)
            run: brew install ffmpeg@7 || brew install ffmpeg
          - name: Install Python deps
            run: |
              python3 -m pip install --upgrade pip
              python3 -m pip install -r requirements-dev.txt
          - name: Pytest
            # Offscreen Qt for macOS so client tests don't require a display
            env:
              QT_QPA_PLATFORM: offscreen
            run: python3 -m pytest -v --timeout=60

      # Synthetic harness-math gate per D-09. Real-hardware latency is measured
      # manually on DXS workstations pre-release. PLACEHOLDER — Plan 16 replaces
      # this job body with the real synthetic-stage-time accumulator benchmark.
      # The job NAME must stay `latency-bench` so D-07 branch protection never
      # needs reconfiguration across the Plan 05 → Plan 16 transition.
      latency-bench:
        name: Latency benchmark (synthetic p99 < 25 ms) — PENDING Plan 16
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - name: Placeholder
            run: |
              echo "PENDING — Plan 16 will replace this with the real benchmark"
              echo "(synthetic-stage-time accumulator per D-09; p99 < 25 ms gate per D-08)"
              exit 0
    ```

    **KEY decisions baked in:**
    - `concurrency.cancel-in-progress: true` — cancel stale PR builds when new commits land (free CI mins)
    - `lint-typecheck` runs on ubuntu-latest (not the Rocky container) for speed — ruff+mypy are pure Python
    - `test-linux` uses `rockylinux:9` container per D-05 + RESEARCH Pitfall 5 for EPEL + python3.12
    - `test-macos` sets `QT_QPA_PLATFORM=offscreen` to prevent client/main.py tests from requiring a display
    - Pip cache keyed on `requirements-*.txt` hash so dep-bumps invalidate correctly
    - No self-hosted runners (D-05)
    - No `actions/setup-python@v5` in the Rocky job — python installed via dnf (RESEARCH Pitfall 5)
    - **`latency-bench` placeholder: exits 0 so green CI is preserved, but the job NAME exists from this plan's first commit so D-07 branch protection can require all four gates immediately (pytest/test-linux, pytest/test-macos, lint-typecheck, build-artifacts/build-mac-app, build-artifacts/build-rpm-dryrun, latency-bench). Plan 16 replaces the stub's body with the real benchmark and removes the "PENDING" suffix from the job display name.**
    - **YAML comment above the latency-bench job documents that this is a synthetic harness-math gate per D-09 (not real hardware). Plan 16's real version keeps this comment verbatim.**

    **Do NOT:**
    - Replace the latency-bench job body with the real benchmark here — Plan 16 owns that (keeps the diff for the latency logic focused)
    - Remove the `# Synthetic harness-math gate per D-09` YAML comment — it must persist across the Plan 05 → Plan 16 transition
    - Add an `actions/upload-artifact@v4` step in test jobs — Plan 05 Task 2's build-artifacts workflow is where artifacts live
    - Use GITHUB_TOKEN explicitly — the default token has the permissions this workflow needs
    - Use `actions/cache@v3` — v4 is current per RESEARCH action table
    - Rename the `latency-bench` job — D-07 branch protection will reference this job name; a rename forces the user to reconfigure the branch rule

    **D-07 branch protection note (for the plan's output SUMMARY):** After this plan lands, the user must enable branch protection in GitHub UI referencing ALL FOUR hard-gate job names from D-08:
    1. `lint-typecheck`
    2. `test-linux` (or the Tests-on-Rocky check name GHA generates)
    3. `test-macos` (or the Tests-on-macOS check name)
    4. `build-mac-app` + `build-rpm-dryrun` (the two jobs in build-artifacts.yml — D-08 gate #3 as a pair)
    5. `latency-bench` (the placeholder stub; Plan 16 replaces the body but keeps the name)

    **Post-write verification:**
    - The YAML must parse with `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` (`pyyaml` will be auto-available once pip deps install; use `python3 -c "import yaml"` after `pip install pyyaml` if needed, or skip and trust GHA's parse on first push).
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "runs-on: macos-14" .github/workflows/ci.yml && grep -q "image: rockylinux:9" .github/workflows/ci.yml && grep -q "actions/checkout@v4" .github/workflows/ci.yml && grep -q "actions/setup-python@v5" .github/workflows/ci.yml && grep -q "actions/cache@v4" .github/workflows/ci.yml && grep -q "epel-release" .github/workflows/ci.yml && grep -q "python3.12" .github/workflows/ci.yml && grep -q "ruff check" .github/workflows/ci.yml && grep -q "ruff format --check" .github/workflows/ci.yml && grep -q "mypy" .github/workflows/ci.yml && grep -q "pytest" .github/workflows/ci.yml && grep -q "QT_QPA_PLATFORM: offscreen" .github/workflows/ci.yml && grep -q "latency-bench:" .github/workflows/ci.yml && grep -q "PENDING — Plan 16" .github/workflows/ci.yml && grep -q "Synthetic harness-math gate per D-09" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists
    - File declares 4 jobs: `lint-typecheck`, `test-linux`, `test-macos`, `latency-bench` (placeholder)
    - `grep -q "runs-on: macos-14" .github/workflows/ci.yml` exits 0 (D-05/D-06)
    - `grep -q "image: rockylinux:9" .github/workflows/ci.yml` exits 0 (D-05)
    - `grep -q "epel-release" .github/workflows/ci.yml` exits 0 (RESEARCH Pitfall 5 — EPEL for python3.12)
    - `grep -q "dnf -y install .* python3.12" .github/workflows/ci.yml` exits 0
    - All four actions pinned at correct majors: `actions/checkout@v4`, `actions/setup-python@v5`, `actions/cache@v4` (upload-artifact comes in Task 2 build-artifacts.yml)
    - `grep -q "concurrency:" .github/workflows/ci.yml` exits 0 (cancel-in-progress)
    - `grep -q "QT_QPA_PLATFORM: offscreen" .github/workflows/ci.yml` exits 0 (client tests don't need a display)
    - `grep -q "ruff format --check" .github/workflows/ci.yml` exits 0 (check mode, not reformatting)
    - `grep -q "latency-bench:" .github/workflows/ci.yml` exits 0 (D-08 gate #4 placeholder installed)
    - `grep -q "PENDING — Plan 16" .github/workflows/ci.yml` exits 0 (placeholder marked explicitly)
    - `grep -q "Synthetic harness-math gate per D-09" .github/workflows/ci.yml` exits 0 (D-09 provenance comment)
    - YAML is syntactically valid (GHA reports no parse error on first push); latency-bench job exits 0 on first run
  </acceptance_criteria>
  <done>ci.yml exists with ALL FOUR D-08 gate job names (lint-typecheck, test-linux + test-macos for pytest, latency-bench placeholder). Build-artifacts gate lives in Task 2's build-artifacts.yml. D-07 branch protection can reference the complete gate set from the first commit; Plan 16 swaps the latency-bench job body without renaming.</done>
</task>

<task type="auto">
  <id>1-05-02</id>
  <name>Task 2: Create .github/workflows/build-artifacts.yml + minimal packaging/teraguchi-server.spec</name>
  <files>.github/workflows/build-artifacts.yml, packaging/teraguchi-server.spec</files>
  <read_first>
    - /Users/randymcentee/workspace/GitHub/teraguchi/.planning/phases/01-stability-ci-test-baseline/01-RESEARCH.md §"Pitfall 4" lines 528-534 — PyInstaller arm64 notes
    - /Users/randymcentee/workspace/GitHub/teraguchi/.planning/phases/01-stability-ci-test-baseline/01-RESEARCH.md Open Question #2 line 1246 — RPM spec decision
    - /Users/randymcentee/workspace/GitHub/teraguchi/install-server.sh (read the dep list to mirror into Requires:)
    - /Users/randymcentee/workspace/GitHub/teraguchi/build_client.py (the existing PyInstaller build script — verify it can run in CI)
    - /Users/randymcentee/workspace/GitHub/teraguchi/server/ directory listing (to know what the RPM packages)
    - /Users/randymcentee/workspace/GitHub/teraguchi/.planning/phases/01-stability-ci-test-baseline/01-PATTERNS.md §"No Analog Found — packaging/teraguchi-server.spec" line 1155
  </read_first>
  <action>
    **Create `packaging/teraguchi-server.spec`** — a minimal Rocky 9 RPM spec that `rpmbuild --nodeps --buildonly` can validate syntactically + structurally without actually installing. This is NOT a signed, distributable RPM — DIST-02 in Phase 6 handles signing + release.

    Rationale (RESEARCH Open Q #2): "ship a minimal spec that builds cleanly" beats "skip the gate entirely" — catches spec-file regressions (missing BuildRequires, busted %files list) before Phase 6 tries to sign it.

    Contents:
    ```spec
    # Phase 1 dry-run RPM spec — NOT for signing or distribution.
    # DIST-02 in Phase 6 adds signing, GPG keys, and real release pipeline.

    Name:           teraguchi-server
    Version:        3.0.0
    Release:        1%{?dist}
    Summary:        Teraguchi remote workstation server — Rocky Linux 9

    License:        ASL 2.0
    URL:            https://github.com/thedepartmentofexternalservices/teraguchi
    Source0:        %{name}-%{version}.tar.gz

    BuildArch:      noarch
    BuildRequires:  python3.12
    BuildRequires:  python3.12-pip

    Requires:       python3.12
    Requires:       ffmpeg-free
    Requires:       xorg-x11-server-Xvfb
    Requires:       pulseaudio-utils

    %description
    Teraguchi server — open-source remote workstation for VFX / Flame workflows.
    Replaces HP Anyware (PCoIP) for small independent VFX studios.

    Phase 1 dry-run spec. Signing + distribution land in Phase 6 (DIST-02).

    %prep
    # Phase 1 dry-run — no actual unpack needed for --buildonly.
    %autosetup -n %{name}-%{version} -c -D 2>/dev/null || true

    %build
    # No compilation step — pure Python. Real packaging lands in Phase 6.
    echo "Phase 1: dry-run only. No artifacts produced."

    %install
    # Dry-run: no install step.
    mkdir -p %{buildroot}%{_bindir}
    # Phase 6 DIST-02 will install a systemd unit + launcher script.

    %files
    # Empty — this spec's purpose is to validate syntax only.

    %changelog
    * Fri Apr 18 2026 Randy McEntee <randy@thedepartmentofexternalservices.com> - 3.0.0-1
    - Phase 1 dry-run RPM spec (no signing, no distribution).
    ```

    **Key decisions:**
    - `License: ASL 2.0` — Fedora/RH's standard name for Apache 2.0 in RPM spec syntax
    - `BuildArch: noarch` — pure Python, no compiled artifacts
    - Empty `%files` — this is a dry-run spec; Phase 6 populates it
    - Requires: mirrors `install-server.sh`'s Rocky dep list (python3.12 + ffmpeg-free + xvfb + pulseaudio-utils)

    **Create `.github/workflows/build-artifacts.yml`** — depends on `test-*` jobs being green (GHA's `needs:` implements this). Runs PyInstaller on macos-14 + rpmbuild dry-run on rockylinux:9:

    ```yaml
    name: Build artifacts

    on:
      pull_request:
        branches: [dev, main]
      push:
        branches: [dev, main]

    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    jobs:
      build-mac-app:
        name: PyInstaller .app (macos-14 arm64 dry-run)
        runs-on: macos-14
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-python@v5
            with:
              python-version: "3.12"
          - uses: actions/cache@v4
            with:
              path: ~/.cache/pip
              key: pip-build-mac-${{ hashFiles('requirements-*.txt') }}
          - name: Install build deps
            run: |
              python3 -m pip install --upgrade pip
              python3 -m pip install -r requirements-client.txt
              python3 -m pip install 'pyinstaller>=6.11,<7'
          - name: PyInstaller build
            # Phase 1 is dry-run only — we gate on exit code, not universal2 or notarization.
            # RESEARCH Pitfall 4: macos-14 runners are arm64; accept arm64-only output.
            run: python3 build_client.py
          - name: Verify .app exists
            run: test -d dist/Teraguchi.app
          - uses: actions/upload-artifact@v4
            with:
              name: Teraguchi-app-arm64
              path: dist/Teraguchi.app
              retention-days: 7

      build-rpm-dryrun:
        name: RPM dry-run (Rocky 9)
        runs-on: ubuntu-latest
        container:
          image: rockylinux:9
        steps:
          - uses: actions/checkout@v4
          - name: Install rpmbuild
            run: |
              dnf -y install epel-release
              dnf -y install rpm-build rpmdevtools python3.12
          - name: Set up rpmbuild tree
            run: |
              rpmdev-setuptree
              # Create a minimal tarball so %prep has something
              mkdir -p /tmp/src/teraguchi-server-3.0.0
              cp -R server/ common/ /tmp/src/teraguchi-server-3.0.0/ || true
              tar -czf ~/rpmbuild/SOURCES/teraguchi-server-3.0.0.tar.gz -C /tmp/src teraguchi-server-3.0.0
              cp packaging/teraguchi-server.spec ~/rpmbuild/SPECS/
          - name: rpmbuild --nodeps --buildonly
            # --nodeps: runner container may not have ffmpeg-free available; Phase 6 fixes.
            # --buildonly: validate spec syntax + %prep/%build without %install actions.
            run: rpmbuild -bb --nodeps ~/rpmbuild/SPECS/teraguchi-server.spec
          - name: Verify SRPM exists
            run: |
              # Even with --nodeps, rpmbuild -bb produces an SRPM as a side-effect
              ls ~/rpmbuild/SRPMS/ || true
              ls ~/rpmbuild/RPMS/noarch/ || true
              echo "Phase 1 RPM dry-run complete (spec validates)"
    ```

    **Rationale for the dry-run semantics:**
    - `rpmbuild -bb --nodeps` builds from the spec without requiring runtime deps to exist on the container (ffmpeg-free is in Requires but we don't want CI to pull it). This catches syntax errors in the spec without failing on missing runtime deps.
    - `%files` is empty so no payload produced — that's intentional; Phase 6 populates it when real distribution lands.
    - The `Verify .app exists` step on macOS gates exit code. We DO NOT run `.app` to verify it launches (GHA runners can't reliably do a window-inspect; manual verification covered in VALIDATION.md §"Manual-Only Verifications").

    **Do NOT:**
    - Try to sign or notarize the `.app` here — Phase 6 owns that (DIST-01 + DIST-06)
    - Populate `%files` with actual file paths — that's Phase 6's work
    - Use `actions/upload-artifact@v3` — v4 is current
    - Ship a GPG key in this repo — Phase 6 publishes the key separately
  </action>
  <verify>
    <automated>test -f .github/workflows/build-artifacts.yml && test -f packaging/teraguchi-server.spec && grep -q "runs-on: macos-14" .github/workflows/build-artifacts.yml && grep -q "image: rockylinux:9" .github/workflows/build-artifacts.yml && grep -q "rpmbuild" .github/workflows/build-artifacts.yml && grep -q "python3 build_client.py" .github/workflows/build-artifacts.yml && grep -q "upload-artifact@v4" .github/workflows/build-artifacts.yml && grep -q "^Name:           teraguchi-server$" packaging/teraguchi-server.spec && grep -q "License:        ASL 2.0" packaging/teraguchi-server.spec && grep -q "BuildArch:      noarch" packaging/teraguchi-server.spec && grep -q "Requires:       ffmpeg-free" packaging/teraguchi-server.spec</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/build-artifacts.yml` exists with 2 jobs: `build-mac-app` + `build-rpm-dryrun`
    - `packaging/teraguchi-server.spec` exists
    - `grep -q "Name:           teraguchi-server" packaging/teraguchi-server.spec` exits 0
    - `grep -q "License:        ASL 2.0" packaging/teraguchi-server.spec` exits 0 (Apache 2.0 in RPM syntax)
    - `grep -q "BuildArch:      noarch" packaging/teraguchi-server.spec` exits 0
    - `grep -q "Requires:       ffmpeg-free" packaging/teraguchi-server.spec` exits 0 (mirrors install-server.sh)
    - `grep -q "python3 build_client.py" .github/workflows/build-artifacts.yml` exits 0
    - `grep -q "rpmbuild -bb --nodeps" .github/workflows/build-artifacts.yml` exits 0
    - `grep -q "upload-artifact@v4" .github/workflows/build-artifacts.yml` exits 0
    - `grep -q "test -d dist/Teraguchi.app" .github/workflows/build-artifacts.yml` exits 0 (exit-code gate)
  </acceptance_criteria>
  <done>build-artifacts.yml + RPM spec land; first CI run on a branch with all prior plans merged produces a green `build-mac-app` and `build-rpm-dryrun`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → PyPI | Supply chain boundary; pip TLS handles integrity |
| GHA runner → Docker Hub | Supply chain boundary; rockylinux:9 is the official community image |
| Unsigned .app artifact | Phase 1 produces unsigned bundles; not intended for distribution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-CI-01 | Tampering | GitHub Actions workflow | accept | Workflow files live in repo; any change requires PR. Actions pinned at major versions (v4/v5). Phase 6 may add SHA-pinning for release-critical actions. |
| T-1-CI-02 | Denial of Service | Docker Hub rate limits for rockylinux:9 pulls | accept (monitor) | RESEARCH Pitfall 5 notes; if CI starts failing on pull limits, pin by digest or mirror via ghcr.io. Not a blocker today. |
| T-1-CI-03 | Tampering | unsigned .app bundle uploaded as artifact | accept | Phase 1 artifact is not distributable; retention 7 days; clearly labeled "arm64 dry-run". Phase 6 DIST-01 adds signing + notarization. |
</threat_model>

<verification>
1. `yamllint .github/workflows/` — both files pass (once yamllint installed; or rely on GHA's parser on first push)
2. First PR after this plan merges triggers 4 jobs in `ci.yml` + 2 jobs in `build-artifacts.yml` = 6 green checks (the latency-bench placeholder is one of the 4 ci.yml jobs and exits 0)
3. `rpmlint packaging/teraguchi-server.spec` (optional local check; lives in `dnf install rpmlint` on Rocky) — zero errors; warnings acceptable
4. User must manually configure branch protection in GitHub UI after this plan lands (D-07) — settings → branches → require status checks to pass; select all four hard-gate job names: `lint-typecheck`, `test-linux`, `test-macos`, `build-mac-app`, `build-rpm-dryrun`, `latency-bench`. The latency-bench job is currently a stub that exits 0; Plan 16 replaces its body without renaming the job so the branch-protection rule never needs reconfiguration.
</verification>

<success_criteria>
- ci.yml + build-artifacts.yml + teraguchi-server.spec exist in the repo
- ALL FOUR D-08 hard-gate job names present in ci.yml from the first commit (lint-typecheck, test-linux, test-macos, latency-bench) — the latency-bench body is a placeholder stub that exits 0
- build-artifacts.yml adds the build-mac-app + build-rpm-dryrun pair (D-08 gate #3)
- D-05 locked: only macos-14 + rockylinux:9 runners
- Planner-open RPM-spec decision (RESEARCH Open Q #2) resolved: ship minimal spec
- D-07 branch protection is a MANUAL user step flagged in VALIDATION + the SUMMARY; the user can enable it immediately because all four gate job names already exist
- Plan 16 replaces the latency-bench placeholder body (synthetic-stage-time accumulator) without renaming the job — zero reconfiguration of D-07 needed
</success_criteria>

<output>
After completion, create `.planning/phases/01-stability-ci-test-baseline/01-05-SUMMARY.md` noting:
- First CI run URL on the branch
- Which jobs passed / failed / were skipped (the latency-bench placeholder should be green with an "echo PENDING" output)
- Any unexpected pytest failures that surface (tests collected but failing) — these get triaged into Plan 04 follow-ups
- Explicit reminder: user must set branch protection in GitHub UI after plan lands (D-07) — reference ALL FOUR gate job names including `latency-bench`, which is a Plan 05 placeholder that Plan 16 replaces without renaming
</output>
</output>
