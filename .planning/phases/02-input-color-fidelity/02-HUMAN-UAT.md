---
status: partial
phase: 02-input-color-fidelity
source: [02-VERIFICATION.md]
started: 2026-04-19T21:16:11Z
updated: 2026-04-19T21:16:11Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. DXS 4-cell Wacom hardware matrix (INPUT-09 / INPUT-10 / INPUT-11 / INPUT-12)

expected:
For each of 4 cells (Intuos Pro Large × {Sonoma, Sequoia} + Cintiq Pro 24 × {Sonoma, Sequoia}):
- Run Teraguchi end-to-end with Phase 2 HEAD build
- Execute the D-17 6-step protocol (pressure ramp, eraser flip, tilt, proximity cycle, side buttons, reconnect mid-stroke)
- Capture client_<cell>.jsonl and server_<cell>.jsonl from structlog stream
- Run `python tools/wacom_quant_analysis.py --client-log client_<cell>.jsonl --server-log server_<cell>.jsonl --cell <cell> --out-svg artifacts/rms_<cell>.svg --pass-threshold 0.01` — must exit 0 (RMS < 1%)
- Record video of each cell's session
- Flame paint-stroke qualitative sign-off on the Cintiq Pro 24 + Sequoia cell

Fill the 4-cell matrix table + sign-off line in `docs/release.md`.

result: [pending]

### 2. DXS pre/post-Phase-2 input-to-photon latency comparison (D-21 / VIDEO-11)

expected:
1. Checkout HEAD-of-Phase-1-verification-complete commit
2. Start Teraguchi; run 3 trial sessions (~60s typical Flame interaction each); record p50/p95/p99 input-to-photon latencies via Phase 1 OBS-02 telemetry
3. Checkout Phase 2 HEAD; repeat
4. Compute median p99 across the 3 trials, pre and post
5. Fill the 3×3 latency comparison table in `docs/release.md`
6. **Regression gate**: post-Phase-2 p99 must be ≤ pre-Phase-2 p99 × 1.15

Bonus: confirm sub-20ms p99 on at least one trial with compatible Wacom + good Tailscale LAN (VIDEO-11 happy path).

result: [pending]

### 3. INPUT-08 IOHIDUserDevice spike re-attempt with real Wacom + Photoshop

expected:
Per 02-10's planned D-07 outcome contract: D-08 PASS bar = pressure visible in Photoshop / Preview / a simple NSView app reads non-zero NSEvent.pressure from across a complete pen stroke, generated by an unsigned Python process using pyobjc-framework-IOKit + IOHIDUserDevice.

The autonomous executor took the FAIL branch (cannot interactively verify Photoshop pressure from a worktree). If you can demonstrate PASS on real hardware:
- Productionize `server/mac_pen_injector.py` (currently absent; module skeleton + 5 mock-at-IOKit tests already drafted in 02-10)
- Update `docs/release.md` IOHIDUserDevice spike section to PASS
- Wire dispatch in `server/platform_backends.py`

If FAIL stands: the documented v1 limitation in `docs/release.md` is the final state — Mac-server pen pressure unsupported in v1; Flame on Rocky stays the production path.

result: [pending]

### 4. Visual 10-bit color confirmation on MBP XDR (D-01 cp.8)

expected:
Run Teraguchi client on a MacBook Pro with XDR display. Stream the 10-bit ramp test fixture (`tests/smoke/fixtures/10bit_ramp.p010.bin`) through the full pipeline. Visually confirm no banding in the smooth gradient regions (a banding-detector test pattern was added in 02-01).

This is checkpoint 8 of the 9-checkpoint VIDEO-02 fixture and is `manual-verified-once` per the plan contract — it is not automatable. cps 2/3/4/5/6/7 are already automated GREEN.

result: [pending]

### 5. Crazy-hotkeys against real Rocky 9 server AND real Mac server

expected:
The `tests/integration/test_crazy_hotkeys.py` 28-case sweep (FLAME_CRITICAL_CHORDS + Cmd↔Ctrl swap + dead-key composition) is GREEN against an in-process loopback server. Confirm the same chord set works against:
- A real Rocky 9 Flame workstation (Linux server with xtest_injector)
- A real Mac server (mac_input_injector with CGEvent)

Specifically test in a live Flame paint session: paint, deform, F-key chords, modifier-held drags. Watch for any modifier-stuck or chord-mangled behavior.

result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
