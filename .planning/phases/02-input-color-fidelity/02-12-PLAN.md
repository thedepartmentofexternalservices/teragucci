---
phase: 02-input-color-fidelity
plan: 12
plan_id: 02-12
type: execute
wave: 9
depends_on: [02-01, 02-02, 02-06, 02-07, 02-08, 02-09, 02-10, 02-11]
files_modified:
  - tools/wacom_quant_analysis.py
  - docs/release.md
  - tests/server/test_wacom_matrix_emission.py
  - requirements-dev.txt
autonomous: false
requirements: [INPUT-09, INPUT-10, INPUT-11, INPUT-12, VIDEO-11]
user_setup: []

must_haves:
  truths:
    - "tools/wacom_quant_analysis.py is no longer a stub -- it reads client+server JSONL, aligns timestamps, computes RMS, writes an SVG, exits 0 iff RMS < threshold."
    - "D-17 structlog counter emission schema is declared + unit-tested (event='wacom_matrix', stage='...', counter=...)."
    - "docs/release.md contains the 4-cell Wacom matrix runbook + the pre/post-Phase-2 DXS latency comparison table."
    - "Real 4-cell matrix session is captured in a manual checkpoint; per-cell RMS numbers committed to docs/release.md."
    - "Phase 1's 4 CI gates remain green; synthetic latency gate has not regressed."
  artifacts:
    - path: "tools/wacom_quant_analysis.py"
      provides: "Fully implemented argparse CLI: numpy + scipy.interpolate timestamp align + RMS + matplotlib SVG, <= 200 lines"
      contains: "interp1d|rms"
    - path: "docs/release.md"
      provides: "4-cell Wacom matrix runbook + pre/post-Phase-2 DXS latency table"
      contains: "Intuos Pro L|Cintiq Pro 24|DXS"
    - path: "tests/server/test_wacom_matrix_emission.py"
      provides: "structlog emission schema contract tests (event=wacom_matrix)"
  key_links:
    - from: "docs/release.md"
      to: "tools/wacom_quant_analysis.py"
      via: "per-release runbook instruction"
      pattern: "wacom_quant_analysis"
---

<objective>
Close the Wacom hardware-verification leg of Phase 2 (INPUT-09..12) and the one-shot DXS real-hardware pre/post-Phase-2 latency measurement (VIDEO-11 + D-21). Replace the Wave-0 `tools/wacom_quant_analysis.py` stub with the real implementation; declare + unit-test the D-17 structlog emission schema; seed `docs/release.md` with the runbook that Phase 6 will formalize. Execute the physical matrix session (manual checkpoint) and commit the per-cell RMS numbers + video link.

Purpose: prove the end-to-end promise of the phase on real hardware. D-16's cadence is "one-shot gate + per-release ritual"; this plan executes the gate.

Output: real `wacom_quant_analysis.py`; emission-schema contract tests; seeded `docs/release.md`; captured per-cell JSONL + RMS + video; verified DXS latency number commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-input-color-fidelity/02-CONTEXT.md
@.planning/phases/02-input-color-fidelity/02-RESEARCH.md
@.planning/phases/02-input-color-fidelity/02-PATTERNS.md
@.planning/phases/02-input-color-fidelity/02-VALIDATION.md

<interfaces>
From .planning/phases/02-input-color-fidelity/02-RESEARCH.md section "D-17 structlog JSON emission shape":
```
{"event":"wacom_matrix","stage":"pressure_ramp","cell":"intuos-pro-L-sonoma",
 "timestamp_ns":1745000000000000000,"counter":"client_pressure_samples","n":237}
{"event":"wacom_matrix","stage":"pressure_ramp","cell":"intuos-pro-L-sonoma",
 "timestamp_ns":1745000000016000000,"client_pressure":0.452,"client_tilt_x":-12.0, ...}
{"event":"wacom_matrix","stage":"pressure_ramp","cell":"intuos-pro-L-sonoma",
 "timestamp_ns":1745000000016100000,"server_hid_pressure_raw":462,
 "server_hid_pressure_normalized":0.4512, ...}
```

Wave 0 stub from 02-01:
```
python tools/wacom_quant_analysis.py \
    --client-log client.jsonl --server-log server.jsonl \
    --cell intuos-pro-L-sonoma --out-svg rms.svg --pass-threshold 0.01
# currently exits 2 (stub); this plan makes it functional.
```

Phase 1 DXS latency harness reference: `tests/smoke/test_latency_benchmark.py` + the per-stage structlog events (capture/encode/transmit/decode/display).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace tools/wacom_quant_analysis.py stub with real RMS + SVG implementation</name>
  <files>tools/wacom_quant_analysis.py, tests/server/test_wacom_matrix_emission.py, requirements-dev.txt</files>
  <read_first>
    - tools/wacom_quant_analysis.py (Wave 0 stub)
    - tools/keydiag.py (argparse CLI shape reference)
    - .planning/phases/02-input-color-fidelity/02-RESEARCH.md section "D-17" + "D-18"
    - requirements-dev.txt (confirm matplotlib not yet pinned)
  </read_first>
  <behavior>
    - `python tools/wacom_quant_analysis.py --client-log <client.jsonl> --server-log <server.jsonl> --cell <cell-name> --out-svg <path> --pass-threshold 0.01` with non-empty well-formed inputs computes per-sample RMS error and exits 0 if RMS < threshold, 1 otherwise.
    - Aligns timestamps via `scipy.interpolate.interp1d` on `timestamp_ns`.
    - Produces an SVG plot with matplotlib: client pressure vs time overlaid on server pressure vs time, annotated with the RMS value and the cell name.
    - Handles missing/empty logs gracefully with exit code 2 + a clear message.
    - Script total line count <= 200.
    - Two unit tests: RMS on a synthetic ramp returns ~0.0; RMS on a known offset returns >= 0.05.
  </behavior>
  <action>
Replace the entire body of `tools/wacom_quant_analysis.py` (overwrite the Wave-0 stub):

```
#!/usr/bin/env python3
"""Wacom pressure-quantization RMS analysis (Phase 2 D-18).

Ingests two side-by-side structlog JSONL streams (client and server), aligns
server pressure samples to client sample timestamps via linear interpolation,
computes RMS error normalized to max pressure, and produces an SVG plot.

Exit codes:
  0  RMS < threshold (pass)
  1  RMS >= threshold (fail)
  2  missing / malformed inputs
"""
from __future__ import annotations

import argparse
import json
import pathlib
import sys
from typing import List, Tuple

import numpy as np


def _read_wacom_samples(path: pathlib.Path, cell: str) -> Tuple[np.ndarray, np.ndarray, str]:
    """Return (timestamps_ns, pressures, schema_tag) filtered by cell + stage."""
    if not path.exists():
        return np.array([]), np.array([]), "missing"
    ts: List[int] = []
    pr: List[float] = []
    schema = "unknown"
    with path.open() as f:
        for line in f:
            try:
                r = json.loads(line)
            except json.JSONDecodeError:
                continue
            if r.get("event") != "wacom_matrix":
                continue
            if r.get("cell") != cell:
                continue
            if "client_pressure" in r:
                schema = "client"
                ts.append(int(r["timestamp_ns"]))
                pr.append(float(r["client_pressure"]))
            elif "server_hid_pressure_normalized" in r:
                schema = "server"
                ts.append(int(r["timestamp_ns"]))
                pr.append(float(r["server_hid_pressure_normalized"]))
    return np.array(ts, dtype=np.int64), np.array(pr, dtype=np.float64), schema


def compute_rms(client_t: np.ndarray, client_p: np.ndarray,
                server_t: np.ndarray, server_p: np.ndarray) -> float:
    """Align server samples to client timestamps via linear interp; RMS of the difference."""
    if client_t.size == 0 or server_t.size == 0:
        return float("nan")
    from scipy.interpolate import interp1d
    # Sort each stream by time, unique-sort in case of duplicates
    ci = np.argsort(client_t); client_t, client_p = client_t[ci], client_p[ci]
    si = np.argsort(server_t); server_t, server_p = server_t[si], server_p[si]
    t_min = max(client_t[0],  server_t[0])
    t_max = min(client_t[-1], server_t[-1])
    if t_max <= t_min:
        return float("nan")
    mask = (client_t >= t_min) & (client_t <= t_max)
    ct = client_t[mask].astype(np.float64)
    cp = client_p[mask]
    if ct.size == 0:
        return float("nan")
    f_srv = interp1d(server_t.astype(np.float64), server_p,
                     kind="linear", assume_sorted=True, bounds_error=False,
                     fill_value="extrapolate")
    sp = f_srv(ct)
    diff = cp - sp
    return float(np.sqrt(np.mean(diff * diff)))


def _render_svg(out_path: pathlib.Path, cell: str, rms: float,
                client_t: np.ndarray, client_p: np.ndarray,
                server_t: np.ndarray, server_p: np.ndarray) -> None:
    import matplotlib
    matplotlib.use("Agg")
    import matplotlib.pyplot as plt
    fig, ax = plt.subplots(figsize=(10, 4))
    if client_t.size:
        t0 = client_t[0]
        ax.plot((client_t - t0) / 1e9, client_p, label="client (QTabletEvent)", linewidth=1.0)
    if server_t.size:
        t0s = client_t[0] if client_t.size else server_t[0]
        ax.plot((server_t - t0s) / 1e9, server_p, label="server (HID report)", linewidth=1.0)
    ax.set_xlabel("time (s)")
    ax.set_ylabel("normalized pressure")
    ax.set_title(f"{cell} | RMS = {rms:.4f}")
    ax.legend()
    ax.grid(alpha=0.3)
    fig.tight_layout()
    fig.savefig(out_path, format="svg")
    plt.close(fig)


def main(argv=None) -> int:
    p = argparse.ArgumentParser(description=__doc__)
    p.add_argument("--client-log", required=True)
    p.add_argument("--server-log", required=True)
    p.add_argument("--cell", required=True)
    p.add_argument("--out-svg", required=True)
    p.add_argument("--pass-threshold", type=float, default=0.01)
    args = p.parse_args(argv)
    client_t, client_p, c_schema = _read_wacom_samples(pathlib.Path(args.client_log), args.cell)
    server_t, server_p, s_schema = _read_wacom_samples(pathlib.Path(args.server_log), args.cell)
    if c_schema == "missing" or s_schema == "missing":
        print(f"ERROR: missing log file(s) for cell={args.cell}", file=sys.stderr)
        return 2
    if client_t.size == 0 or server_t.size == 0:
        print(f"ERROR: no wacom_matrix events for cell={args.cell}", file=sys.stderr)
        return 2
    rms = compute_rms(client_t, client_p, server_t, server_p)
    _render_svg(pathlib.Path(args.out_svg), args.cell, rms,
                client_t, client_p, server_t, server_p)
    print(f"cell={args.cell} RMS={rms:.4f} threshold={args.pass_threshold:.4f}")
    if not (rms == rms):  # NaN check
        return 2
    return 0 if rms < args.pass_threshold else 1


if __name__ == "__main__":
    sys.exit(main())
```

Add to `requirements-dev.txt` (preserve existing lines; append only):
```
matplotlib>=3.8
scipy>=1.14
numpy>=2.1
```

Create `tests/server/test_wacom_matrix_emission.py`:

```
"""D-17 structlog emission schema contract tests + D-18 RMS unit tests."""
import json
import pathlib
import tempfile

import numpy as np
import pytest


def test_rms_on_identical_ramp_is_near_zero():
    from tools.wacom_quant_analysis import compute_rms
    t = np.arange(0, 1000, 10, dtype=np.int64) * 1_000_000  # 10ms steps, ns units
    p = np.linspace(0, 1, t.size)
    rms = compute_rms(t, p, t, p)
    assert rms < 1e-6


def test_rms_on_offset_ramp_reflects_offset():
    from tools.wacom_quant_analysis import compute_rms
    t = np.arange(0, 1000, 10, dtype=np.int64) * 1_000_000
    cp = np.linspace(0, 1, t.size)
    sp = cp + 0.05
    rms = compute_rms(t, cp, t, sp)
    assert abs(rms - 0.05) < 1e-6


def test_rms_with_missing_stream_returns_nan():
    from tools.wacom_quant_analysis import compute_rms
    import math
    rms = compute_rms(np.array([]), np.array([]), np.array([1,2]), np.array([0.1, 0.2]))
    assert math.isnan(rms)


def test_main_exits_2_on_missing_input(tmp_path):
    from tools.wacom_quant_analysis import main
    rc = main([
        "--client-log", str(tmp_path / "missing_c.jsonl"),
        "--server-log", str(tmp_path / "missing_s.jsonl"),
        "--cell", "intuos-pro-L-sonoma",
        "--out-svg", str(tmp_path / "out.svg"),
        "--pass-threshold", "0.01",
    ])
    assert rc == 2


def test_main_exits_0_on_matched_rampy_log(tmp_path):
    """End-to-end: produce synthetic client+server JSONL with a matched ramp; assert exit 0."""
    from tools.wacom_quant_analysis import main
    client_log = tmp_path / "client.jsonl"
    server_log = tmp_path / "server.jsonl"
    with client_log.open("w") as fc, server_log.open("w") as fs:
        base = 1_745_000_000 * 1_000_000_000
        for i, p in enumerate(np.linspace(0, 1, 100)):
            fc.write(json.dumps({
                "event": "wacom_matrix", "cell": "intuos-pro-L-sonoma",
                "timestamp_ns": base + i * 10_000_000,
                "client_pressure": float(p),
            }) + "\n")
            fs.write(json.dumps({
                "event": "wacom_matrix", "cell": "intuos-pro-L-sonoma",
                "timestamp_ns": base + i * 10_000_000,
                "server_hid_pressure_normalized": float(p) + 0.0001,  # near-perfect match
            }) + "\n")
    rc = main([
        "--client-log", str(client_log), "--server-log", str(server_log),
        "--cell", "intuos-pro-L-sonoma", "--out-svg", str(tmp_path / "rms.svg"),
        "--pass-threshold", "0.01",
    ])
    assert rc == 0
    assert (tmp_path / "rms.svg").exists()


def test_main_exits_1_on_known_offset(tmp_path):
    from tools.wacom_quant_analysis import main
    client_log = tmp_path / "client.jsonl"
    server_log = tmp_path / "server.jsonl"
    with client_log.open("w") as fc, server_log.open("w") as fs:
        base = 1_745_000_000 * 1_000_000_000
        for i, p in enumerate(np.linspace(0, 1, 100)):
            fc.write(json.dumps({
                "event": "wacom_matrix", "cell": "intuos-pro-L-sequoia",
                "timestamp_ns": base + i * 10_000_000,
                "client_pressure": float(p),
            }) + "\n")
            fs.write(json.dumps({
                "event": "wacom_matrix", "cell": "intuos-pro-L-sequoia",
                "timestamp_ns": base + i * 10_000_000,
                "server_hid_pressure_normalized": float(p) + 0.05,  # 5% offset -> RMS = 0.05
            }) + "\n")
    rc = main([
        "--client-log", str(client_log), "--server-log", str(server_log),
        "--cell", "intuos-pro-L-sequoia", "--out-svg", str(tmp_path / "rms.svg"),
        "--pass-threshold", "0.01",
    ])
    assert rc == 1
```

Run `pytest tests/server/test_wacom_matrix_emission.py -x -q`.
  </action>
  <verify>
    <automated>pytest tests/server/test_wacom_matrix_emission.py -x -q &amp;&amp; python tools/wacom_quant_analysis.py --help | grep -qE "RMS|quant" &amp;&amp; test $(wc -l &lt; tools/wacom_quant_analysis.py) -le 250</automated>
  </verify>
  <acceptance_criteria>
    - `tools/wacom_quant_analysis.py` line count <= 250 (target 200; cap 250 for margin).
    - `tools/wacom_quant_analysis.py` imports `numpy` AND `scipy.interpolate` (grep).
    - `tools/wacom_quant_analysis.py` imports `matplotlib` (grep).
    - `pytest tests/server/test_wacom_matrix_emission.py -x` exits 0 (6 tests).
    - `requirements-dev.txt` lists `matplotlib`, `scipy`, `numpy`.
    - `python tools/wacom_quant_analysis.py --help` prints usage and exits 0.
  </acceptance_criteria>
  <done>wacom_quant_analysis.py is real; 6 unit tests green; deps pinned.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the 4-cell DXS Wacom matrix (INPUT-09..12) per D-17 protocol</name>
  <what-built>
Real-hardware Wacom matrix session at DXS. Not CI-runnable.

Physical setup: DXS lab with:
  - Cell 1: Intuos Pro Large + macOS Sonoma
  - Cell 2: Intuos Pro Large + macOS Sequoia
  - Cell 3: Cintiq Pro 24 + macOS Sonoma
  - Cell 4: Cintiq Pro 24 + macOS Sequoia

For each cell, run Teraguchi end-to-end with the Phase 2 HEAD build; exercise the D-17 6-step protocol; capture `client.jsonl` + `server.jsonl`; compute RMS per cell; record video.
  </what-built>
  <how-to-verify>
For each of the 4 cells:

1. **Pressure ramp** (INPUT-12): pen draws a slow 0→max stroke over ~5 seconds. Confirm client-side log captures >= 200 `wacom_matrix` pressure samples with `client_pressure in (0, 1.0]`.

2. **Eraser flip** (INPUT-10): turn pen over, stroke. Confirm client log shows events with `client_pointer_type == "eraser"` and `client_pressure > 0`.

3. **Tilt test**: hold pen tilted while stroking. Confirm at least one event has `|client_tilt_x| > 10` or `|client_tilt_y| > 10`.

4. **Proximity cycle** (INPUT-11): lift pen out of range and back in. Confirm server structlog shows PenFSM transition `out_of_proximity → in_proximity` AND `in_proximity → out_of_proximity`.

5. **Tablet-side buttons** (INPUT-10): press each side button while stroking. Confirm server structlog has button-press events for both side buttons at least once.

6. **Reconnect mid-stroke**: during step 1, disconnect the WebSocket; reconnect within 5 seconds. Confirm post-reconnect: server PenFSM is `out_of_proximity`; no orphaned TeraguchiTablet in `ioreg -l -c IOHIDUserDevice`; first post-auth message was KEY_RESET_MODIFIERS(reason="reconnect").

7. **Run RMS analysis**: `python tools/wacom_quant_analysis.py --client-log client_<cell>.jsonl --server-log server_<cell>.jsonl --cell <cell> --out-svg artifacts/rms_<cell>.svg --pass-threshold 0.01`. Must exit 0 (RMS < 1%).

8. **Record video** of each cell's session; link in docs/release.md.

9. **Flame qualitative sign-off**: Flame artist draws a standard paint stroke on Cintiq Pro 24 + Sequoia; signs off that quantization artifacts are not visible. This qualitative step is MANDATORY per D-17.

Pass gate: ALL 4 cells pass ALL 6 automated steps + RMS < 1% per cell + qualitative sign-off on the Cintiq Pro 24 + Sequoia cell.

Record in `docs/release.md` Phase 2 section:
- Matrix execution date
- Per-cell pass/fail + RMS numbers
- Video links
- Flame artist sign-off signature
  </how-to-verify>
  <resume-signal>
Reply with one of:
  - `MATRIX PASS` + paste of the 4-cell RMS table + video links + sign-off note.
  - `MATRIX FAIL cell=<X> reason=<Y>` + description of the failure mode.
    On fail: triage -- is it a Phase 2 implementation bug (fix and re-run) or a real hardware driver issue (document as a v1 caveat in docs/release.md)?
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: DXS pre/post-Phase-2 latency measurement (VIDEO-11 / D-21)</name>
  <what-built>
One-shot real-hardware end-to-end input-to-photon latency measurement on DXS:
  - Client: Mac Studio (or MacBook Pro) on LAN Tailscale.
  - Server: Rocky Flame workstation.
  - Peripherals: Real Wacom + real Cintiq + real display.

Compare HEAD-of-dev-at-Phase-1-end (pre-Phase-2) vs HEAD-of-Phase-2 (post).
  </what-built>
  <how-to-verify>
1. Checkout `HEAD-of-Phase-1-verification-complete` commit (see `.planning/phases/01-stability-ci-test-baseline/01-VERIFICATION.md` commit ref). Start Teraguchi. Run 3 trial sessions, each capturing `structlog` per-stage latency telemetry (Phase 1 OBS-02) end-to-end for ~60 seconds of typical Flame interaction. Record p50 / p95 / p99 end-to-end input-to-photon latencies.

2. Checkout Phase 2 HEAD. Repeat. Record the same metrics.

3. Compute the median p99 across the 3 trials, pre and post. Commit to `docs/release.md` Phase 2 section as:

```
| Metric | Pre-Phase-2 (Phase 1 HEAD) | Post-Phase-2 | Delta |
|--------|---------------------------|--------------|-------|
| p50 input-to-photon (ms)  |  X  |  Y  |  +/- Z |
| p95 input-to-photon (ms)  |  X  |  Y  |  +/- Z |
| p99 input-to-photon (ms)  |  X  |  Y  |  +/- Z |
```

4. **Regression gate (D-21):** if post-Phase-2 p99 > pre-Phase-2 p99 * 1.15, DO NOT sign off the phase. Triage. Common culprits: QRhiWidget blit cost, VTCompressionSession lifecycle overhead, capability-probe startup cost (one-shot; shouldn't affect per-frame).

Pass gate: p99 regression within +15%.

Bonus: confirm that with a compatible hardware Wacom + good Tailscale LAN, p99 measures under 20ms on at least one trial (VIDEO-11 happy path). If it does NOT, document as a known caveat -- the CI *gate* (pass/fail) cites Phase 1 synthetic p99 < 25ms (D-08/D-09; unchanged in Phase 2 per D-21). Real-hardware DXS target remains sub-20ms per CLAUDE.md core value, but CI pass/fail is the 25ms number.
  </how-to-verify>
  <resume-signal>
Reply with one of:
  - `LATENCY PASS p99_pre=<X>ms p99_post=<Y>ms delta=<Z>%` + copy of the table inserted into docs/release.md.
  - `LATENCY FAIL p99_pre=<X>ms p99_post=<Y>ms delta=<Z>%` + triage note (which stage regressed: capture/encode/transmit/decode/display?).
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Finalize docs/release.md with Phase 2 release section</name>
  <files>docs/release.md</files>
  <read_first>
    - docs/release.md (current state; either seeded in 02-10 Task 2 or 02-10 FAIL branch)
  </read_first>
  <action>
Ensure `docs/release.md` has a Phase 2 section that serves as the Phase 6 release-runbook seed. Structure:

```
# Teraguchi Release Runbook

## Phase 2 -- Input + Color Fidelity

### Wacom matrix ritual (per-release)

Run before tagging any `v*` release. Requires DXS lab access.

1. For each of 4 cells (Intuos Pro Large + Cintiq Pro 24 x macOS Sonoma + Sequoia):
   - Execute the D-17 6-step protocol.
   - Capture `client_<cell>.jsonl` and `server_<cell>.jsonl` from the structlog stream.
   - Run `python tools/wacom_quant_analysis.py --client-log ... --server-log ... --cell <cell> --out-svg artifacts/rms_<cell>.svg --pass-threshold 0.01`.
   - Record video of the session; link below.

2. Fail the release if ANY cell RMS >= 1% OR ANY protocol step assertion fails.

3. Flame paint-stroke qualitative sign-off (artist signs off on the Cintiq Pro 24 + Sequoia cell).

### Phase 2 matrix results (<MATRIX EXECUTION DATE>)

| Cell | RMS | Pass/Fail | Video |
|------|-----|-----------|-------|
| Intuos Pro Large + Sonoma  | <filled by Task 2> | ... | ... |
| Intuos Pro Large + Sequoia | ... | ... | ... |
| Cintiq Pro 24 + Sonoma     | ... | ... | ... |
| Cintiq Pro 24 + Sequoia    | ... | ... | ... |

Flame artist sign-off: <name + date>

### Phase 2 latency measurement (D-21)

| Metric | Pre-Phase-2 (Phase 1 HEAD) | Post-Phase-2 | Delta |
|--------|---------------------------|--------------|-------|
| p50 input-to-photon (ms)  |  <filled>  |  <filled>  |  <+/-> |
| p95 input-to-photon (ms)  |  ...       |  ...       |  ...   |
| p99 input-to-photon (ms)  |  ...       |  ...       |  ...   |

Synthetic-stage CI gate (Phase 1 D-08/D-09 p99 < 25ms; unchanged per D-21): still green.

### IOHIDUserDevice spike outcome (D-07)

- Outcome: <PASS / FAIL> (recorded <date>)
- <details from 02-10 Task 0 resume-signal>

### Known v1 caveats emerging from Phase 2

- <any caveat noted during matrix / latency / spike>
```

Commit the file with the Task 2 + Task 3 outcomes filled in.
  </action>
  <verify>
    <automated>grep -qE "Wacom matrix ritual|Phase 2 matrix results|Phase 2 latency measurement" docs/release.md &amp;&amp; grep -q "IOHIDUserDevice spike outcome" docs/release.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/release.md` contains all four sections (matrix ritual, matrix results, latency table, spike outcome).
    - Matrix results table has 4 cells populated with real numbers (or placeholder `<DEFERRED to v1.1 per ...>` if Task 2 was a `MATRIX FAIL`).
    - Latency table has 3 metrics x 3 columns populated.
  </acceptance_criteria>
  <done>Phase 6 release-runbook seed committed with real Phase 2 matrix + latency numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tools/wacom_quant_analysis.py | Operator-local tool; ingests structlog JSONL from the session |
| docs/release.md | Markdown doc; read by human maintainers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-34 | Information-Disclosure | Wacom matrix JSONL contains raw pressure/tilt samples; may be committed to a public release | accept | The JSONL artifacts stay in `artifacts/` (developer-local). Only the SVG plot + summary numbers make it to docs/release.md. Document in the runbook: do not commit raw JSONL to the public repo. |
| T-02-35 | Tampering | Adversary tampers with committed RMS numbers in docs/release.md | accept | Standard code-review + signed-release path (Phase 6). Phase 2 numbers are a one-time commitment; manipulation post-hoc is caught by commit history + Phase 6 signature checks. |
| T-02-36 | DoS | Malformed JSONL crashes wacom_quant_analysis.py | mitigate | `_read_wacom_samples` silently skips malformed lines + returns empty arrays; `main()` returns exit 2 with an error message. |
</threat_model>

<verification>
- `pytest tests/server/test_wacom_matrix_emission.py -x` exits 0 (6 tests).
- `python tools/wacom_quant_analysis.py --help` exits 0.
- `docs/release.md` contains populated matrix + latency tables.
- Phase 1's 4 CI gates still green (no regression).
- Synthetic latency p99 < 25ms still green (Phase 1 D-08/D-09 gate; unchanged per D-21).
- All Phase 2 plans 01-11 SUMMARYs confirm green.
</verification>

<success_criteria>
- `tools/wacom_quant_analysis.py` is production: RMS + SVG + exit code semantics.
- D-17 emission schema unit-tested.
- 4-cell matrix executed (MATRIX PASS) with per-cell RMS < 1%, or FAIL triaged + documented.
- DXS pre/post latency measured, delta within +15% of pre-Phase-2 p99.
- docs/release.md seeded with the Phase 6 runbook.
</success_criteria>

<output>
Create `.planning/phases/02-input-color-fidelity/02-12-SUMMARY.md`: matrix table (per-cell RMS + video links), latency table (pre/post/delta), spike outcome summary, link to the seeded docs/release.md.
</output>
